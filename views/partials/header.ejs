<!DOCTYPE html>
<nav>
    <div>
        <img src="/images/logo-1color.svg" alt="Logo" class="logo">
        <div>
        <a href="/"><img src="/images/app-name.svg" class="logotext" alt="Logotext"></a>
       <% if (loggedInUser) { %>
            <p>@<%= loggedInUser.username %></p>
            <% } else { %>
                <p>Gast</p>
            <% } %>
        </div>
    </div>
      <a href="<%= loggedInUser ? `/user/${loggedInUser._id}` : '/login' %>">
    <img class="pfp" src="<%= loggedInUser ? `/pfp/${loggedInUser._id}` : '/images/pfp.jpg' %>" alt="profielfoto">
</a>
</nav>
